<nav class="navbar">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "GameDB", home_path, class: "navbar-brand" %>
    </div>
    <ul class="nav navbar-nav">
      <% if (!user_signed_in?) %>
        <li><%= link_to "Log in", new_user_session_path, class: "btn" %></li>
        <li><%= link_to "Sign up", new_user_registration_path, class: "btn" %></li>
      <% else %>
        <li><%= link_to 'New Game', new_game_path, class: "btn" %></li>
        <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to sign out?' }, class: "btn" %></li>
      <% end %>
    </ul>
  </div>
</nav>
